{
  "name": "radiology-transcription-api-gateway",
  "version": "1.0.0",
  "description": "API Gateway for Radiology Transcription Service",
  "main": "direct_start.js",
  "scripts": {
    "start": "node direct_start.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
